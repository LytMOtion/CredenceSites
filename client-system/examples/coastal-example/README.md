# coastal-example (example coastal input)

A committed EXAMPLE of a filled client-work folder. Copy its files as a starting
point, or run `npm run client:new -- --system coastal --slug your-slug` for a blank one.
See client-system/docs/CLIENT-SYSTEM-GUIDE.md.
